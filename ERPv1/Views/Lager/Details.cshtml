@model ERPv1.Models.Lager

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Lager")">Übersicht</a></li>
    <li class="breadcrumb-item active">Details</li>
</ol>

<div class="card">
    <div class="card-header">
        Lager Details
    </div>
    <div class="card-body detail-body">
        @Html.Partial("_LagerInput", Model)

        <div class="flex-row card-buttons">
            @Html.ActionLink("Bearbeiten", "Edit", new { id = Model?.ID }, new { @class = "btn btn-primary" })
            @Html.ActionLink("Löschen", "Delete", new { id = Model?.ID }, new { @class = "btn btn-danger" })
            @Html.ActionLink("Waren hinzufügen", "AddProducts", new { id = Model?.ID }, new { @class = "btn btn-secondary" })
            @Html.ActionLink("Zurück zur Liste", "Index", null, new { @class = "btn btn-white text-dark" })
        </div>
    </div>
</div>