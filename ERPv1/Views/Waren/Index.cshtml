@using ERPv1.Extensions
@model IEnumerable<ERPv1.Models.Ware>

@{
    ViewBag.Title = "Waren Übersicht";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<ol class="breadcrumb">
    <li class="breadcrumb-item active">Übersicht</li>
</ol>

<div>

    <h4>@ViewBag.Title</h4>

    <hr />

    <p>
        @Html.ActionLinkWithIcon("Neue Ware erstellen", "fa fa-plus-circle", true, "Create", "Waren", null, new { @class = "btn btn-primary", style = "margin-left:0;" })
    </p>

    <table id="indexTable" class="table table-striped table-bordered" style="width: 100%; padding-left: 0; padding-right: 0;">
        <thead>
            <tr>
                <th class="no-pointer"></th>
                <th>@Html.DisplayNameFor(x => x.ArtikelNummer)</th>
                <th>@Html.DisplayNameFor(x => x.Bezeichnung)</th>
                <th>@Html.DisplayNameFor(x => x.KurzBezeichnung)</th>
                <th>@Html.DisplayNameFor(x => x.HerstellerName)</th>
                <th>@Html.DisplayNameFor(x => x.EinzelPreis)</th>
                <th>@Html.DisplayNameFor(x => x.Anzahl)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var ware in Model)
            {
                <tr>
                    <td><a href="@Url.Action("Details", "Waren", new { id = ware.ID })"><span class="badge badge-default button-column" style="padding: 5px">Details</span></a></td>
                    <td>@ware.ArtikelNummer</td>
                    <td>@ware.Bezeichnung</td>
                    <td>@ware.KurzBezeichnung</td>
                    <td>@ware.HerstellerName</td>
                    <td>@ware.EinzelPreis.ToEuro()</td>
                    <td>@ware.Anzahl</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@Scripts.Render("~/Scripts/ERPScripts/DataTablesConfig.js")
